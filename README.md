# HTML共有くん

HTML共有くんは、AIコーディングエージェントに作らせたHTMLを1か所へためて、スマホからも読めるようにするツールです。自分のAWSアカウントで動かすセルフホスト型で、作者へページや回答が送られることはありません。

<p align="center">
  <img src="docs/images/dashboard.png" alt="HTML共有くんのメインダッシュボード" width="66%">
  &nbsp;
  <img src="docs/images/mobile-approval-actions-native.png" alt="AIから届いた承認依頼をスマホで確認" width="31%">
</p>

## 主な機能

### ためる

- メモや調査結果を、同梱スキル `create-html` で見やすいHTMLに整える
- 作ったHTMLを自分専用の一覧に登録して配信する。画像やフォントは1枚のHTMLへ埋め込むので、配信先でリンクが切れない
- 配信は差分だけを送る。変わったページを上げ、消えたページを消す。2つの配信が重なってもページが消えないよう、生成から送信までを排他する

### 一覧で見つける

- 同じ案件のページを1枚のカードにまとめ、更新の新しい順に並べる
- カード、または個別ページにスターを付け、ヘッダーの「★ スター」チップで絞り込む
- まだ開いていないページを新着として示す。読んだページは「未読に戻す」で新着へ戻せる
- 設定ファイルの `content.shelf` に書いた進行中の仕事を、ヘッダーの絞り込みチップとして締切の近い順に並べる（[進行中の棚](#進行中の棚)）
- タイトルとリポジトリ名で一覧を絞り込む
- 不要なページは一覧から外せる。原本と発行済みのURLは残り、「削除済み」から戻せる
- スター・既読・削除・棚から下ろした印はPCとスマホで共有される

### スマホで読む

- ホーム画面に追加すると、アプリのように開ける
- はみ出す表は縦積みのカードへ畳み、月カレンダーは1列に並べ直す
- 画面上端から引っ張って更新する
- 入力欄に触れても画面が拡大しない

### 共有する

- 個別ページの「共有URLを発行」から、期限付きURLをその場で発行してコピーする
- 組織の出口IPを設定しておくと、社内限定のURLも選べる
- 有効期限の上限は設定で決める。無期限のURLは作らない
- 生成したHTMLは管理画面と別のオリジンへ置き、検索エンジンにも載せない
- 共有URLをSlackやTeamsへ貼ると、ページ名と冒頭の1文がカードで表示される

## 進行中の棚

ヘッダーの2段目に絞り込みチップを並べ、まだ終わっていない仕事を1つずつチップにします。並びは「すべて」「★ スター」（スター付きのページかカードがあるときだけ）、棚の項目の順です。一覧そのものは日付の区切りだけで、スターや進行中の段は置きません。ページ単位ではなく仕事単位なので、ページの無い用件（チャットの返信待ちなど）も載せられます。台帳は設定ファイルの `content.shelf` で、エージェントに「棚に載せて」「棚から外して」と頼めば出し入れできます。

```yaml
content:
  shelf:
    - id: demo-review          # 一意なID。✕で下ろした印はこのIDで覚える
      title: デモレポートの見直し
      stream: demo             # チップを押すと一覧をこのテーマに絞る
      due: 2026-01-31          # 締切（任意）
      note: 数字の出典を確認する
    - id: team-thread
      title: チャットの問い合わせに返信
      url: https://chat.example.com/threads/12345   # ページの無い用件はリンクで
      added: 2026-01-20
```

- 並びは締切の近い順です。締切の無い項目は後ろへ回り、最近動いた順に並びます
- チップには名前と残り日数（「今日」「明日」「5日」。3日以内は強調色）を出します。締切の無いテーマはページ数を出します
- `due` の翌日を過ぎた項目と `done: true` の項目は、`publish` の時点で棚から落ちます
- テーマ（`stream`）のチップを押すと、一覧をそのテーマのページだけに絞り込みます。畳まれていた版も全件出ます。同じチップをもう一度押すか「すべて」で解除します。リンク（`url`）のチップはリンクを新しいタブで開きます（↗ 付き）
- 絞り込み中は本文の先頭に、名前・締切（「あと5日・1/31（土）」）・メモ・ページ数を1行で出します
- 選択中のチップの右端の ✕ で棚から下ろすと、その印は本人設定としてPCとスマホで共有されます。設定ファイルとページはそのまま残ります
- 検索中と削除済みの表示ではチップを出さず、絞り込みも解除します
- PCではチップを折り返し、スマホ幅では横スクロールで並べます

## リンクプレビュー（OGP）

共有URLをSlack・Teams・Xへ貼ったとき、素っ気ないURLではなくカードで表示されます。

| 出るもの | 中身 |
|---|---|
| アプリ名 | `content.siteName`（省略時は `#HTML共有くん`） |
| ページ名 | 設定の `title`、無ければHTMLの `<title>` |
| 説明文 | ヒーローのリード文（`<p class="sub">`）→ `lead` → 最初の段落の順に拾う |
| 画像 | 同梱のカード画像（`assets/og-card.jpg`）。`content.ogImageUrl` で差し替え、`false` で出さない |
| カードの大きさ | `content.ogCardType`（省略時は `summary`＝各媒体でいちばん小さいカード） |

画像は次の3通りです。

- **同梱の画像（既定）**：publish が配信先の `/og/card.jpg` へ置き、全ページのカードに使います。
  配信面は署名付きURLでしか開けませんが、`/og/*` だけは署名なしで開ける設定にしてあるので、
  クローラーも画像を取れます。置くのはこの1枚だけで、ページの中身は `/og/` に置きません
- **自前の画像**：`content.ogImageUrl` に絶対URL（https）を書くと、そちらを使います。
  `siteName` を変えたときは、画像の中のアプリ名と揃うようにこちらで差し替えてください
- **画像なし**：`content.ogImageUrl: false` にすると `og:image` を出しません。
  SlackとXはテキストだけのカードになり、**Teamsは`og:image`が無いとカードを出しません**

既存の環境で同梱の画像を使い始めるには、`deploy`（`/og/*` の配信ルールを足す）と
`publish` の両方が要ります。

ページが自分で `og:title` を持っている場合は、そのページには何も足しません。

### 画像を置くときの注意

**`og:image` はページ本体とは別のリクエストで取りに来ます。タグが正しくても、画像だけ落ちます。**
しかも落ちたことは表に出ず、**Xは大きいカードを諦めて小さいカードへ縮む**だけなので、
「なぜかOGPが出ない」に見えて原因がHTML側にありません。

- **画像URLを直接叩いて、`200` かつ `Content-Type: image/*` が返ることを確かめる。**
  S3 + CloudFront OAC は「オブジェクトが無い」を **404 ではなく 403** で返すので、
  権限の問題だと読み違えやすい
- **`Cache-Control: immutable` を付けない。**「中身は永久に変わらない」の宣言になり、
  画像を差し替えてもクローラーが取り直しません。`public, max-age=3600` 程度が無難
- **差し替えたら、URLの末尾に `?v=20260920` のようなクエリを足す。**
  SNSは画像をURLの文字列で覚えるので、ファイル名が同じままだと古い画像が出続けます。
  **ファイル名のほうを変えると、既存の投稿が参照している画像が消えます**
- **正方形の画像は、要素を中央の横帯に収めて描く。** 同じ画像でも、パソコン版のSlackは
  小さな正方形のサムネイルで、スマホ版のSlackは横長に切り抜いた大きな画像で出します。
  クローラーは1回しか取りに来ないので、表示先ごとに画像を出し分けることはできません。
  上下いっぱいに文字を積むと、スマホ版で上下が詰まって見えます。アプリ名は1行にし、
  上下に余白を残すと、どちらの表示でも収まります
- **`robots.txt` でクローラーを止めない。** 検索インデックスからの除外は `X-Robots-Tag` と
  `<meta name="robots">` が担っています。`Disallow: /` を置くと Slack / Teams / X の
  クローラーまで巻き添えになります
- SVGは使えません。JPEG か PNG を置いてください

### カードの大きさ

既定は `summary`（各媒体でいちばん小さいカード）です。**画像を指定しても大きくなりません。**

```yaml
content:
  ogCardType: summary_large_image   # 横長の大きなカードにしたいときだけ
```

`summary_large_image` は **1200×630 前後の横長画像**を前提にした見せ方です。
正方形やロゴを渡すと引き伸ばされます。ページごとに別々の画像を用意しない運用では、
小さいカードのほうが収まりよく見えます。

> メタタグは `<meta charset>` の後ろへ入れています。日本語のページ名が文字コード宣言より前に出ると、
> クローラー側が文字化けすることがあるためです。

## 対応エージェント

中身はCLIとスキルなので、Claude Code、Codex、Cursorなど手元のどのエージェントからでも同じように呼び出せます。

## スマホとの連携

- 思いついた作業は、スマホのインボックスへ置いておける。戻って `/inbox` と頼めばエージェントが引き取る
- 逆向きの `/mobile` は、PCの確認依頼をスマホへ送る。外出先で返した承認やコメントはPC側へ引き継がれる

> [!NOTE]
> Claude Codeのリモートコントロールとは仕組みが違います。あちらは母艦のPCでセッションを起動したままにしておく必要がありますが、共有くんのやり取りは非同期です。作業をスマホへ送ったあとはPCの電源を切ってよく、外出先で返した回答は、次にPCを開いたときに続きとして取り込めます。

## 頼み方の例

セットアップが済んだあとは、いつもどおりエージェントへ日本語で頼みます。コマンドは覚えなくてOKです。

> このHTMLを共有くんに追加して

> このページを社内限定で7日間共有して

> `/inbox` で、スマホから置いた依頼を引き取って

## セットアップ

導入方法は [初回セットアップ](docs/setup.md) にまとめています。仕組みは [アーキテクチャ](docs/architecture.md)、公開前に確認したい点は [セキュリティ設計](docs/threat-model.md) を参照してください。

## ライセンス

Apache License 2.0
